# Core dependencies (numerical methods + plotting + tests)
numpy>=1.20
matplotlib>=3.4
pytest>=7.0

# Optional: enables the `--symbolic` exact-value comparison in the CLI.
# Uncomment (or run `pip install sympy`) if you want it.
# sympy>=1.9
