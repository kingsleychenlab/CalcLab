# The presence of this file at the project root puts the repository root on
# sys.path during test collection, so `import calclab` works when you simply
# run `pytest` from a fresh clone (no install step required).
